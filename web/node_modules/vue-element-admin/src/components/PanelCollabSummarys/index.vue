<template>
  <div class="card-panel">
    <div class="card-panel-icon-wrapper">
      <font-awesome-icon :style="{color: iconColor}" :icon="icon" size="4x"/>
    </div>
    <div class="card-panel-description">
      <slot/>
      <div class="card-panel-text">{{ description }}</div>
    </div>
  </div>
</template>

<script>
export default {
  props: {
    icon: {
      type: String,
      required: true
    },
    iconColor: {
      type: String,
      required: true
    },
    // backgroudColor: {
    //   type: String,
    //   required: true
    // },
    description: {
      type: String,
      required: true
    }
  }
}
</script>

<style rel="stylesheet/scss" lang="scss" scoped>
  .panel-group {
    margin-top: 2px;
    .card-panel-col {
      margin-bottom: 15px;
    }
    .card-panel {
      height: 100px;
      cursor: pointer;
      font-size: 12px;
      position: relative;
      overflow: hidden;
      color: #666;
      background: #fff;
      box-shadow: 4px 4px 40px rgba(0, 0, 0, .05);
      border-color: rgba(0, 0, 0, .05);
      .icon-users {
        color: #40c9c6;
      }
      .icon-magic {
        color: #36a3f7;
      }
      .icon-chart {
        color: #f4516c;
      }
      .icon-shopping {
        color: #34bfa3
      }
      .card-panel-icon-wrapper {
        float: left;
        margin: 14px 0 0 14px;
        padding: 16px;
        transition: all 0.38s ease-out;
        border-radius: 6px;
      }
      .card-panel-icon {
        float: left;
        font-size: 48px;
      }
      .card-panel-description {
        text-align: right;
        float: right;
        font-weight: bold;
        margin: 26px;
        margin-left: 0px;
        .card-panel-num {
          font-size: 30px;
        }
        .card-panel-text {
          text-align: right;
          line-height: 18px;
          color: rgba(0, 0, 0, 0.45);
          font-size: 14px;
          margin-top: 4px;
        }
      }
    }
  }
</style>
