import request from '../default-request'
// require('../mocking/test-api')
// This sets the mock adapter on the default instance
const getUsers = function(callback, errorHandler) {
  const path = '/users'
  request.getRequest(path, callback, errorHandler)
}

export default {
  getUsers
}
