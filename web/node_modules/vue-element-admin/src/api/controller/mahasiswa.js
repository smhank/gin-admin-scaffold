import request from '../default-request'

const getStudents = function(callback, errorHandler) {
  const path = '/master/mahasiwa/all'
  request.getRequest(path, callback, errorHandler)
}

export default { getStudents }
